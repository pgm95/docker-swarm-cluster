secrets:
  # Core Authelia secrets (versioned)
  authelia_jwt_secret:
    name: authelia_jwt_secret_${DEPLOY_VERSION}
    external: true
  authelia_oidc_hmac_secret:
    name: authelia_oidc_hmac_secret_${DEPLOY_VERSION}
    external: true
  authelia_session_secret:
    name: authelia_redis_session_secret_${DEPLOY_VERSION}
    external: true
  authelia_storage_encryption_key:
    name: authelia_storage_encryption_key_${DEPLOY_VERSION}
    external: true
  authelia_oidc_jwks_key:
    name: authelia_oidc_jwks_key_${DEPLOY_VERSION}
    external: true
  authelia_ldap_bind_user:
    name: authelia_ldap_bind_user_${DEPLOY_VERSION}
    external: true
  authelia_ldap_bind_pass:
    name: authelia_ldap_bind_pass_${DEPLOY_VERSION}
    external: true
  authelia_postgres_password:
    name: authelia_postgres_password_${DEPLOY_VERSION}
    external: true

  # Bootstrap secrets (versioned)
  carpal_ldap_bind_pass:
    name: carpal_ldap_bind_pass_${DEPLOY_VERSION}
    external: true

  # LLDAP secrets (versioned)
  lldap_jwt_secret:
    name: lldap_jwt_secret_${DEPLOY_VERSION}
    external: true
  lldap_key_seed:
    name: lldap_key_seed_${DEPLOY_VERSION}
    external: true
  lldap_ldap_user_pass:
    name: lldap_ldap_user_pass_${DEPLOY_VERSION}
    external: true

  # Shared SMTP secrets (versioned)
  smtp_user:
    name: global_smtp_user_${DEPLOY_VERSION}
    external: true
  smtp_pass:
    name: global_smtp_pass_${DEPLOY_VERSION}
    external: true

  # OIDC client secrets (versioned)
  oidc_client_secret_stirlingpdf:
    name: oidc_client_secret_stirlingpdf_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_paperless:
    name: oidc_client_secret_paperless_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_mealie:
    name: oidc_client_secret_mealie_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_dawarich:
    name: oidc_client_secret_dawarich_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_quantum:
    name: oidc_client_secret_quantum_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_proxmox:
    name: oidc_client_secret_proxmox_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_immich:
    name: oidc_client_secret_immich_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_portainer:
    name: oidc_client_secret_portainer_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_uptimekuma:
    name: oidc_client_secret_uptimekuma_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_grafana:
    name: oidc_client_secret_grafana_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_tailscale:
    name: oidc_client_secret_tailscale_${DEPLOY_VERSION}
    external: true
  oidc_client_secret_cloudflare:
    name: oidc_client_secret_cloudflare_${DEPLOY_VERSION}
    external: true
