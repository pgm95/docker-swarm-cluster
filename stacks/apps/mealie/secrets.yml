secrets:
  oidc_client_secret_mealie:
    name: oidc_client_secret_mealie_${DEPLOY_VERSION}
    external: true
  mealie_openai_api_key:
    name: mealie_openai_api_key_${DEPLOY_VERSION}
    external: true
  smtp_pass:
    name: global_smtp_pass_${DEPLOY_VERSION}
    external: true
