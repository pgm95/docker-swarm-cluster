secrets:
  imap_server:
    name: imap_server_${DEPLOY_VERSION}
    external: true
  smtp_server:
    name: smtp_server_${DEPLOY_VERSION}
    external: true
  email_address:
    name: email_address_${DEPLOY_VERSION}
    external: true
  email_password:
    name: email_password_${DEPLOY_VERSION}
    external: true
  fwd_recipient_1:
    name: fwd_recipient_1_${DEPLOY_VERSION}
    external: true
  fwd_recipient_2:
    name: fwd_recipient_2_${DEPLOY_VERSION}
    external: true
  fwd_recipient_3:
    name: fwd_recipient_3_${DEPLOY_VERSION}
    external: true
