secrets:
  dawarich_db_password:
    name: dawarich_db_password_${DEPLOY_VERSION}
    external: true
  dawarich_backup_db_password:
    name: dawarich_backup_db_password_${DEPLOY_VERSION}
    external: true

configs:
  init_backup_entrypoint:
    name: dawarich_init_backup_entrypoint_${DEPLOY_VERSION}
    file: ./config/bootstrap/init-backup.sh
