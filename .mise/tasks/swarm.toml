#:schema https://mise.jdx.dev/schema/mise-task.json

["swarm:deploy"]
description = "Deploy a stack with automatic secret management"
usage = 'arg "<path>" help="Path to stack directory"'
env.CONVERGE_TIMEOUT = "180"
env.CONVERGE_INTERVAL = "2"
run = '''
#!/usr/bin/env bash
prepare_out=$(python3 -m swarm.deploy prepare "${usage_path}") || exit 1
eval "${prepare_out}"
echo "Deploying: ${STACK_NAME}"
if ! python3 -m swarm._compose "${STACK_PATH}/compose.yml" | sed '/^name:/d; s/published: "\([0-9]*\)"/published: \1/' | docker stack deploy --detach --with-registry-auth -c - "${STACK_NAME}"; then
    echo "ERROR: Deploy command failed"
    [[ -n "${DEPLOY_VERSION:-}" ]] && echo "Tip: Run 'mise run swarm:cleanup' to remove orphaned secrets"
    exit 1
fi
echo ""
python3 -m swarm.convergence wait "${STACK_NAME}" --timeout "${CONVERGE_TIMEOUT}" --interval "${CONVERGE_INTERVAL}"
python3 -m swarm.convergence check "${STACK_NAME}"
echo ""
docker stack services "${STACK_NAME}"
if [[ -n "${DEPLOY_VERSION:-}" ]]; then
    echo ""
    echo "Deployed with DEPLOY_VERSION=${DEPLOY_VERSION}"
fi
'''

["swarm:validate"]
description = "Internal: validate all compose files for Swarm compatibility"
hide = true
run = 'python3 -m swarm.validate'

["swarm:remove"]
description = "Remove a deployed stack and wait for services to drain"
usage = 'arg "<path>" help="Path to stack directory"'
run = 'python3 -m swarm.remove "${usage_path}"'

["swarm:status"]
alias = "status"
description = "Show cluster nodes and health status of all deployed stacks"
run = 'python3 -m swarm.status'

["swarm:cleanup"]
description = "Remove unused versioned secrets, configs, dead containers, and dangling images"
usage = 'flag "--prune-images" help="Also prune all unused images on every node"'
run = 'python3 -m swarm.cleanup'

["swarm:init-networks"]
description = "Internal: create overlay networks (idempotent)"
hide = true
env.SWARM_INTERNAL_NETWORKS = "infra_socket"
run = 'python3 -m swarm.networks init'
